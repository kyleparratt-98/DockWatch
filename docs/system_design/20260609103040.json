{"Implementation approach":"我们将构建一个专注于Docker容器资源争用的实时监控CLI工具。核心挑战在于：1) 从cgroup v1/v2和/proc文件系统可靠地收集容器级指标；2) 处理短暂容器和PID回收；3) 在直接文件系统访问失败时优雅降级到Docker API；4) 使用rich库创建直观的实时ASCII热图可视化。\n\n我们将采用以下方法：\n- **指标收集**：使用`psutil`和直接文件读取来解析`/sys/fs/cgroup`和`/proc`路径。对于cgroup v2，我们将遵循统一层次结构。对于I/O等待，我们将使用cgroup的`io.stat`（v2）或`blkio.throttle.io_service_bytes`（v1）来计算I/O压力，而不是复杂的`/proc/stat`聚合。\n- **容器映射**：通过解析`/proc/<pid>/cgroup`和`/proc/<pid>/mountinfo`来关联PIDs与Docker容器ID，并定期刷新映射以处理PID回收。\n- **降级机制**：当检测到权限问题或cgroup路径不一致时，自动切换到使用`docker` SDK（`docker-py`）的`docker stats` API，并在UI中显示警告横幅。\n- **可视化**：利用`rich`库的`Layout`、`Live`、`Panel`和`Table`组件构建动态网格。每个单元格将根据阈值（例如，CPU节流计数>1000为红色）进行颜色编码，并嵌入使用`rich.sparkline`的迷你趋势图。\n- **性能**：使用异步循环（`asyncio`）进行定期数据收集和UI更新，避免阻塞。缓存容器映射以减少开销。\n\n关键开源库：`rich`（终端UI）、`docker`（降级API）、`psutil`（进程/系统指标）、`asyncio`（异步循环）。","File list":["docker_resource_monitor/__init__.py","docker_resource_monitor/__main__.py","docker_resource_monitor/main.py","docker_resource_monitor/metrics/__init__.py","docker_resource_monitor/metrics/collector.py","docker_resource_monitor/metrics/cgroup_parser.py","docker_resource_monitor/metrics/fallback.py","docker_resource_monitor/visualization/__init__.py","docker_resource_monitor/visualization/ui.py","docker_resource_monitor/visualization/heatmap.py","docker_resource_monitor/container/__init__.py","docker_resource_monitor/container/mapper.py","docker_resource_monitor/config.py","docker_resource_monitor/utils.py","pyproject.toml","README.md"],"Data structures and interfaces":"\nclassDiagram\n    class DockerResourceMonitor {\n        -Config config\n        -ContainerMapper container_mapper\n        -MetricCollector metric_collector\n        -VisualizationEngine visualization_engine\n        +run() None\n        -_main_loop() None\n    }\n    class Config {\n        -int refresh_interval\n        -str color_theme\n        -list container_filter\n        -bool use_docker_api\n        -str warning_banner\n        +load_from_file(path: str) None\n        +get_refresh_interval() int\n        +get_color_theme() str\n        +get_container_filter() list\n        +get_use_docker_api() bool\n        +set_use_docker_api(value: bool) None\n        +get_warning_banner() str\n        +set_warning_banner(value: str) None\n    }\n    class ContainerMapper {\n        -dict pid_to_container_cache\n        -float last_refresh_time\n        -int cache_ttl\n        +get_container_for_pid(pid: int) dict\n        +refresh_cache() None\n        -_parse_cgroup(pid: int) dict\n        -_get_docker_id_from_cgroup(cgroup_path: str) str\n    }\n    class MetricCollector {\n        -ContainerMapper container_mapper\n        -FallbackCollector fallback_collector\n        -bool use_fallback\n        +collect_metrics() dict\n        -_collect_from_cgroup() dict\n        -_collect_io_wait() dict\n        -_get_cgroup_version() int\n    }\n    class FallbackCollector {\n        -docker.DockerClient docker_client\n        +collect_metrics() dict\n        -_parse_docker_stats() dict\n    }\n    class VisualizationEngine {\n        -rich.layout.Layout layout\n        -rich.live.Live live_display\n        -HeatmapRenderer heatmap_renderer\n        +update(data: dict) None\n        +start() None\n        +stop() None\n        -_create_layout() rich.layout.Layout\n        -_update_warning_banner() None\n    }\n    class HeatmapRenderer {\n        -Config config\n        +render(data: dict) rich.table.Table\n        -_get_color_for_value(metric: str, value: float) str\n        -_create_sparkline(values: list) rich.sparkline.Sparkline\n    }\n    DockerResourceMonitor --> Config\n    DockerResourceMonitor --> ContainerMapper\n    DockerResourceMonitor --> MetricCollector\n    DockerResourceMonitor --> VisualizationEngine\n    MetricCollector --> ContainerMapper\n    MetricCollector --> FallbackCollector\n    VisualizationEngine --> HeatmapRenderer\n    HeatmapRenderer --> Config\n","Program call flow":"\nsequenceDiagram\n    participant M as DockerResourceMonitor\n    participant C as Config\n    participant CM as ContainerMapper\n    participant MC as MetricCollector\n    participant FC as FallbackCollector\n    participant VE as VisualizationEngine\n    participant HR as HeatmapRenderer\n    M->>C: load_from_file(\"config.toml\")\n    M->>CM: __init__()\n    M->>MC: __init__(container_mapper=CM)\n    M->>VE: __init__()\n    M->>VE: start()\n    loop Main Loop\n        M->>MC: collect_metrics()\n        alt Direct Filesystem Access Successful\n            MC->>CM: get_container_for_pid(pid)\n            CM-->>MC: return container_info\n            MC->>MC: _collect_from_cgroup()\n            MC->>MC: _collect_io_wait()\n            MC-->>M: return metrics_dict\n        else Direct Access Fails\n            MC->>C: get_use_docker_api()\n            C-->>MC: return True\n            MC->>FC: collect_metrics()\n            FC->>FC: _parse_docker_stats()\n            FC-->>MC: return metrics_dict\n            MC->>C: set_warning_banner(\"Falling back to Docker API\")\n        end\n        M->>VE: update(metrics_dict)\n        VE->>C: get_warning_banner()\n        C-->>VE: return banner_text\n        VE->>VE: _update_warning_banner()\n        VE->>HR: render(metrics_dict)\n        HR->>C: get_color_theme()\n        C-->>HR: return theme\n        HR-->>VE: return rich.table.Table\n        VE->>VE: Refresh Live Display\n        M->>M: sleep(refresh_interval)\n    end\n    M->>VE: stop()\n","Anything UNCLEAR":"需求中提到从`/proc`获取'I/O wait percentages'。需要澄清：这是指从`/proc/stat`中聚合每个容器的`%iowait`（这很复杂），还是使用cgroup中的更简单指标，如`blkio`压力？我们假设使用cgroup的I/O统计（v2的`io.stat`或v1的`blkio.throttle.io_service_bytes`）来计算I/O压力百分比，因为这是容器级别的且更可行。另外，降级时的'warning banner'的确切格式和更新频率可以进一步指定：我们将使用一个始终显示在顶部的`rich.Panel`，当降级激活时，其内容变为警告信息，否则为空。"}